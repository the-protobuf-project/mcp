// Code generated by protoc-gen-mcp. DO NOT EDIT.
// versions:
// 	protoc-gen-mcp v1.7.1-0.20260818024328-1b8cf63db52c+dirty
// Authors: The Protobuf Project - Open Source 2026
// source: todo/v1/todo_service.proto

use std::sync::Arc;
use async_trait::async_trait;
use rmcp::{ErrorData as McpError, RoleServer, ServerHandler, ServiceExt, model::*, service::{Peer, RequestContext}};
use serde_json::{self, json, Value};

fn make_tool(name: &str, description: &str, schema_json: &str, output_schema_json: &str, annotations: Value, title: &str, icons: Value) -> Tool {
    let mut tool = json!({
        "name": name, "description": description,
        "inputSchema": serde_json::from_str::<Value>(schema_json).unwrap(),
        "outputSchema": serde_json::from_str::<Value>(output_schema_json).unwrap(),
    });
    // A hint the proto does not state stays absent: to a client, "unknown" is
    // not the same as "false".
    if !annotations.is_null() {
        tool["annotations"] = annotations;
    }
    if !title.is_empty() {
        tool["title"] = json!(title);
    }
    if !icons.is_null() {
        tool["icons"] = icons;
    }
    serde_json::from_value(tool).expect("generated tool schema must be valid")
}

fn make_tool_with_app_meta(name: &str, description: &str, schema_json: &str, output_schema_json: &str, annotations: Value, title: &str, icons: Value, app_resource_uri: &str) -> Tool {
    let mut tool = json!({
        "name": name, "description": description,
        "inputSchema": serde_json::from_str::<Value>(schema_json).unwrap(),
        "outputSchema": serde_json::from_str::<Value>(output_schema_json).unwrap(),
        "_meta": { "ui": { "resourceUri": app_resource_uri } }
    });
    if !annotations.is_null() {
        tool["annotations"] = annotations;
    }
    if !title.is_empty() {
        tool["title"] = json!(title);
    }
    if !icons.is_null() {
        tool["icons"] = icons;
    }
    serde_json::from_value(tool).expect("generated tool schema must be valid")
}

fn app_resource_uri(service_name: &str) -> String {
    format!("ui://{}/app.html", service_name.to_lowercase())
}

/// Sends MCP progress notifications for one tool call.
///
/// A server-streaming RPC reports incremental progress through this. When the
/// client sent no progressToken there is nothing to correlate a notification
/// with, so every send is a no-op and an implementation never has to check.
///
/// The `Default` sink is exactly that inert one, which is what a unit test for a
/// streaming RPC wants.
#[derive(Clone, Default)]
pub struct McpProgressSink {
    peer: Option<Peer<RoleServer>>,
    token: Option<ProgressToken>,
}

impl McpProgressSink {
    fn new(peer: Option<Peer<RoleServer>>, token: Option<ProgressToken>) -> Self {
        Self { peer, token }
    }

    /// Reports progress. `total` is the expected final value when known.
    pub async fn send(&self, progress: f64, total: Option<f64>, message: Option<String>) {
        let (Some(peer), Some(token)) = (self.peer.as_ref(), self.token.as_ref()) else {
            return;
        };
        // ProgressNotificationParam is #[non_exhaustive], so it is built through
        // its constructor rather than a struct literal.
        let mut param = ProgressNotificationParam::new(token.clone(), progress);
        if let Some(total) = total {
            param = param.with_total(total);
        }
        if let Some(message) = message {
            param = param.with_message(message);
        }
        // A dropped client should not fail the tool call it is no longer watching.
        let _ = peer.notify_progress(param).await;
    }
}

fn default_app_html(app_name: &str, version: &str, description: &str) -> String {
    format!("<!DOCTYPE html><html lang=\"en\"><head><meta charset=\"utf-8\"><title>{app_name}</title></head><body><h1>{app_name}</h1><p>v{version}</p><p>{description}</p><p>This is a generated MCP App placeholder. Replace this resource with your own UI.</p></body></html>")
}

const TODO_SERVICE__CREATE_TODO_SCHEMA_JSON: &str = r##"{"description":"Creates a new todo item under a user. Requires parent (e.g. users/alice), a todo object with title/description/priority, and a unique todo_id.","properties":{"parent":{"description":"Parent resource name (e.g. users/alice). The todo will be created under this user.","type":"string"},"todo":{"properties":{"completed":{"description":"Whether the todo is done.","type":"boolean"},"create_time":{"format":"date-time","type":["string","null"]},"description":{"description":"Optional longer description or notes.","type":"string"},"name":{"description":"Resource name (e.g. users/alice/todos/abc123). Required when updating.","type":"string"},"priority":{"description":"Priority level for a todo item. PRIORITY_UNSPECIFIED: Unspecified; use default priority.. PRIORITY_LOW: Low priority; can be done when convenient.. PRIORITY_MEDIUM: Normal priority; default for most todos.. PRIORITY_HIGH: High priority; should be done soon.. PRIORITY_URGENT: Urgent; do first.","enum":["PRIORITY_UNSPECIFIED","PRIORITY_LOW","PRIORITY_MEDIUM","PRIORITY_HIGH","PRIORITY_URGENT"],"enumDescriptions":{"PRIORITY_HIGH":"High priority; should be done soon.","PRIORITY_LOW":"Low priority; can be done when convenient.","PRIORITY_MEDIUM":"Normal priority; default for most todos.","PRIORITY_UNSPECIFIED":"Unspecified; use default priority.","PRIORITY_URGENT":"Urgent; do first."},"type":"string"},"title":{"description":"Short title for the todo.","type":"string"},"update_time":{"format":"date-time","type":["string","null"]}},"required":[],"type":"object"},"todo_id":{"description":"Unique ID for the todo (e.g. abc123). Becomes the final segment of the resource name.","examples":["abc123","todo-001"],"type":"string"}},"required":["parent","todo","todo_id"],"type":"object"}"##;
const TODO_SERVICE__CREATE_TODO_OUTPUT_SCHEMA_JSON: &str = r##"{"properties":{"completed":{"description":"Whether the todo is done.","type":"boolean"},"create_time":{"format":"date-time","type":["string","null"]},"description":{"description":"Optional longer description or notes.","type":"string"},"name":{"description":"Resource name (e.g. users/alice/todos/abc123). Required when updating.","type":"string"},"priority":{"description":"Priority level for a todo item. PRIORITY_UNSPECIFIED: Unspecified; use default priority.. PRIORITY_LOW: Low priority; can be done when convenient.. PRIORITY_MEDIUM: Normal priority; default for most todos.. PRIORITY_HIGH: High priority; should be done soon.. PRIORITY_URGENT: Urgent; do first.","enum":["PRIORITY_UNSPECIFIED","PRIORITY_LOW","PRIORITY_MEDIUM","PRIORITY_HIGH","PRIORITY_URGENT"],"enumDescriptions":{"PRIORITY_HIGH":"High priority; should be done soon.","PRIORITY_LOW":"Low priority; can be done when convenient.","PRIORITY_MEDIUM":"Normal priority; default for most todos.","PRIORITY_UNSPECIFIED":"Unspecified; use default priority.","PRIORITY_URGENT":"Urgent; do first."},"type":"string"},"title":{"description":"Short title for the todo.","type":"string"},"update_time":{"format":"date-time","type":["string","null"]}},"required":[],"type":"object"}"##;
const TODO_SERVICE__DELETE_TODO_SCHEMA_JSON: &str = r##"{"description":"Permanently deletes a todo item by its resource name. This action cannot be undone.","properties":{"name":{"description":"Resource name of the todo to delete (e.g. users/alice/todos/abc123).","type":"string"}},"required":["name"],"type":"object"}"##;
const TODO_SERVICE__DELETE_TODO_OUTPUT_SCHEMA_JSON: &str = r##"{"properties":{},"required":[],"type":"object"}"##;
const TODO_SERVICE__GET_TODO_SCHEMA_JSON: &str = r##"{"description":"Retrieves a single todo item by its resource name (e.g. users/alice/todos/abc123).","properties":{"name":{"description":"Resource name of the todo (e.g. users/alice/todos/abc123).","examples":["users/alice/todos/abc123"],"format":"uri","type":"string"}},"required":["name"],"type":"object"}"##;
const TODO_SERVICE__GET_TODO_OUTPUT_SCHEMA_JSON: &str = r##"{"properties":{"completed":{"description":"Whether the todo is done.","type":"boolean"},"create_time":{"format":"date-time","type":["string","null"]},"description":{"description":"Optional longer description or notes.","type":"string"},"name":{"description":"Resource name (e.g. users/alice/todos/abc123). Required when updating.","type":"string"},"priority":{"description":"Priority level for a todo item. PRIORITY_UNSPECIFIED: Unspecified; use default priority.. PRIORITY_LOW: Low priority; can be done when convenient.. PRIORITY_MEDIUM: Normal priority; default for most todos.. PRIORITY_HIGH: High priority; should be done soon.. PRIORITY_URGENT: Urgent; do first.","enum":["PRIORITY_UNSPECIFIED","PRIORITY_LOW","PRIORITY_MEDIUM","PRIORITY_HIGH","PRIORITY_URGENT"],"enumDescriptions":{"PRIORITY_HIGH":"High priority; should be done soon.","PRIORITY_LOW":"Low priority; can be done when convenient.","PRIORITY_MEDIUM":"Normal priority; default for most todos.","PRIORITY_UNSPECIFIED":"Unspecified; use default priority.","PRIORITY_URGENT":"Urgent; do first."},"type":"string"},"title":{"description":"Short title for the todo.","type":"string"},"update_time":{"format":"date-time","type":["string","null"]}},"required":[],"type":"object"}"##;
const TODO_SERVICE__LIST_TODOS_SCHEMA_JSON: &str = r##"{"description":"Lists all todo items for a user. Supports pagination via page_size and page_token.","properties":{"page_size":{"description":"Max number of todos to return (default 50).","type":"integer"},"page_token":{"description":"Token from previous response for next page.","type":"string"},"parent":{"description":"Parent resource name (e.g. users/alice). Lists todos for this user.","type":"string"}},"required":["parent"],"type":"object"}"##;
const TODO_SERVICE__LIST_TODOS_OUTPUT_SCHEMA_JSON: &str = r##"{"properties":{"next_page_token":{"type":"string"},"todos":{"items":{"properties":{"completed":{"description":"Whether the todo is done.","type":"boolean"},"create_time":{"format":"date-time","type":["string","null"]},"description":{"description":"Optional longer description or notes.","type":"string"},"name":{"description":"Resource name (e.g. users/alice/todos/abc123). Required when updating.","type":"string"},"priority":{"description":"Priority level for a todo item. PRIORITY_UNSPECIFIED: Unspecified; use default priority.. PRIORITY_LOW: Low priority; can be done when convenient.. PRIORITY_MEDIUM: Normal priority; default for most todos.. PRIORITY_HIGH: High priority; should be done soon.. PRIORITY_URGENT: Urgent; do first.","enum":["PRIORITY_UNSPECIFIED","PRIORITY_LOW","PRIORITY_MEDIUM","PRIORITY_HIGH","PRIORITY_URGENT"],"enumDescriptions":{"PRIORITY_HIGH":"High priority; should be done soon.","PRIORITY_LOW":"Low priority; can be done when convenient.","PRIORITY_MEDIUM":"Normal priority; default for most todos.","PRIORITY_UNSPECIFIED":"Unspecified; use default priority.","PRIORITY_URGENT":"Urgent; do first."},"type":"string"},"title":{"description":"Short title for the todo.","type":"string"},"update_time":{"format":"date-time","type":["string","null"]}},"required":[],"type":"object"},"type":"array"}},"required":[],"type":"object"}"##;
const TODO_SERVICE__UPDATE_TODO_SCHEMA_JSON: &str = r##"{"description":"Updates an existing todo item. Send the todo with its resource name and the fields to update. Use update_mask to specify which fields to modify.","properties":{"todo":{"properties":{"completed":{"description":"Whether the todo is done.","type":"boolean"},"create_time":{"format":"date-time","type":["string","null"]},"description":{"description":"Optional longer description or notes.","type":"string"},"name":{"description":"Resource name (e.g. users/alice/todos/abc123). Required when updating.","type":"string"},"priority":{"description":"Priority level for a todo item. PRIORITY_UNSPECIFIED: Unspecified; use default priority.. PRIORITY_LOW: Low priority; can be done when convenient.. PRIORITY_MEDIUM: Normal priority; default for most todos.. PRIORITY_HIGH: High priority; should be done soon.. PRIORITY_URGENT: Urgent; do first.","enum":["PRIORITY_UNSPECIFIED","PRIORITY_LOW","PRIORITY_MEDIUM","PRIORITY_HIGH","PRIORITY_URGENT"],"enumDescriptions":{"PRIORITY_HIGH":"High priority; should be done soon.","PRIORITY_LOW":"Low priority; can be done when convenient.","PRIORITY_MEDIUM":"Normal priority; default for most todos.","PRIORITY_UNSPECIFIED":"Unspecified; use default priority.","PRIORITY_URGENT":"Urgent; do first."},"type":"string"},"title":{"description":"Short title for the todo.","type":"string"},"update_time":{"format":"date-time","type":["string","null"]}},"required":[],"type":"object"},"update_mask":{"description":"Comma-separated field names to update (e.g. title,completed). Omit to update all provided fields.","type":"string"}},"required":["todo"],"type":"object"}"##;
const TODO_SERVICE__UPDATE_TODO_OUTPUT_SCHEMA_JSON: &str = r##"{"properties":{"completed":{"description":"Whether the todo is done.","type":"boolean"},"create_time":{"format":"date-time","type":["string","null"]},"description":{"description":"Optional longer description or notes.","type":"string"},"name":{"description":"Resource name (e.g. users/alice/todos/abc123). Required when updating.","type":"string"},"priority":{"description":"Priority level for a todo item. PRIORITY_UNSPECIFIED: Unspecified; use default priority.. PRIORITY_LOW: Low priority; can be done when convenient.. PRIORITY_MEDIUM: Normal priority; default for most todos.. PRIORITY_HIGH: High priority; should be done soon.. PRIORITY_URGENT: Urgent; do first.","enum":["PRIORITY_UNSPECIFIED","PRIORITY_LOW","PRIORITY_MEDIUM","PRIORITY_HIGH","PRIORITY_URGENT"],"enumDescriptions":{"PRIORITY_HIGH":"High priority; should be done soon.","PRIORITY_LOW":"Low priority; can be done when convenient.","PRIORITY_MEDIUM":"Normal priority; default for most todos.","PRIORITY_UNSPECIFIED":"Unspecified; use default priority.","PRIORITY_URGENT":"Urgent; do first."},"type":"string"},"title":{"description":"Short title for the todo.","type":"string"},"update_time":{"format":"date-time","type":["string","null"]}},"required":[],"type":"object"}"##;

#[async_trait]
pub trait TodoServiceMcpServer: Send + Sync + 'static {
    async fn create_todo(&self, args: Value) -> std::result::Result<Value, McpError>;
    async fn delete_todo(&self, args: Value) -> std::result::Result<Value, McpError>;
    async fn get_todo(&self, args: Value) -> std::result::Result<Value, McpError>;
    async fn list_todos(&self, args: Value) -> std::result::Result<Value, McpError>;
    async fn update_todo(&self, args: Value) -> std::result::Result<Value, McpError>;
}

pub struct TodoServiceMcpHandler<T: TodoServiceMcpServer> { inner: Arc<T> }

impl<T: TodoServiceMcpServer> Clone for TodoServiceMcpHandler<T> {
    fn clone(&self) -> Self { Self { inner: Arc::clone(&self.inner) } }
}

impl<T: TodoServiceMcpServer> TodoServiceMcpHandler<T> {
    pub fn new(svc: T) -> Self { Self { inner: Arc::new(svc) } }

    pub fn tools() -> Vec<Tool> {
        let app_uri = app_resource_uri("TodoService");
        vec![
            make_tool_with_app_meta("todo_service-create_todo_v1", "Creates a new todo item under a user. Requires parent (e.g. users/alice), a todo object with title/description/priority, and a unique todo_id.", TODO_SERVICE__CREATE_TODO_SCHEMA_JSON, TODO_SERVICE__CREATE_TODO_OUTPUT_SCHEMA_JSON, Value::Null, "", Value::Null, &app_uri),
            make_tool_with_app_meta("todo_service-delete_todo_v1", "Permanently deletes a todo item by its resource name. This action cannot be undone.", TODO_SERVICE__DELETE_TODO_SCHEMA_JSON, TODO_SERVICE__DELETE_TODO_OUTPUT_SCHEMA_JSON, Value::Null, "", Value::Null, &app_uri),
            make_tool_with_app_meta("todo_service-get_todo_v1", "Retrieves a single todo item by its resource name (e.g. users/alice/todos/abc123).", TODO_SERVICE__GET_TODO_SCHEMA_JSON, TODO_SERVICE__GET_TODO_OUTPUT_SCHEMA_JSON, Value::Null, "", Value::Null, &app_uri),
            make_tool_with_app_meta("todo_service-list_todos_v1", "Lists all todo items for a user. Supports pagination via page_size and page_token.", TODO_SERVICE__LIST_TODOS_SCHEMA_JSON, TODO_SERVICE__LIST_TODOS_OUTPUT_SCHEMA_JSON, Value::Null, "", Value::Null, &app_uri),
            make_tool_with_app_meta("todo_service-update_todo_v1", "Updates an existing todo item. Send the todo with its resource name and the fields to update. Use update_mask to specify which fields to modify.", TODO_SERVICE__UPDATE_TODO_SCHEMA_JSON, TODO_SERVICE__UPDATE_TODO_OUTPUT_SCHEMA_JSON, Value::Null, "", Value::Null, &app_uri),
        ]
    }

    pub fn prompts() -> Vec<Prompt> {
        vec![
            serde_json::from_value(json!({
                "name": "summarize_todos",
                "description": "Summarize all pending todo items for a user",
                "arguments": [
                    {"name": "user", "description": "The user ID to summarize todos for.", "required": true},
                ]
            })).expect("generated prompt must be valid"),
            serde_json::from_value(json!({
                "name": "prioritize_todos",
                "description": "Suggest a priority ordering for a user's incomplete todos",
                "arguments": [
                    {"name": "user", "description": "The user ID whose todos to prioritize.", "required": true},
                    {"name": "strategy", "description": "Prioritization strategy.", "required": false},
                ]
            })).expect("generated prompt must be valid"),
        ]
    }

    pub fn completion_map() -> std::collections::HashMap<String, Vec<String>> {
        let mut m: std::collections::HashMap<String, Vec<String>> = std::collections::HashMap::new();
        m.insert(
            "prioritize_todos:strategy".to_string(),
            vec!["urgency".to_string(),"deadline".to_string(),"effort".to_string(),],
        );
        m
    }

    pub fn resources() -> Vec<Resource> {
        vec![
            serde_json::from_value(json!({
                "uri": app_resource_uri("TodoService"),
                "name": "Todo App",
                "mimeType": "text/html"
            })).expect("generated app resource must be valid"),
        ]
    }

    pub fn resource_templates() -> Vec<ResourceTemplate> {
        vec![
            serde_json::from_value(json!({
                "uriTemplate": "todo://users/{user}/todos/{todo}", "name": "Todo",
                "description": "Todo resource (users/{user}/todos/{todo})", "mimeType": "application/json"
            })).expect("generated resource template must be valid"),
        ]
    }
}

impl<T: TodoServiceMcpServer> ServerHandler for TodoServiceMcpHandler<T> {
    fn get_info(&self) -> ServerInfo {
        ServerInfo::new(
            ServerCapabilities::builder()
                .enable_tools()
                .enable_prompts()
                .enable_completions()
                .enable_resources()
                .build(),
        )
        .with_server_info(Implementation::new("TodoService", "0.1.0"))
    }

    async fn list_tools(&self, _: Option<PaginatedRequestParams>, _: RequestContext<RoleServer>) -> std::result::Result<ListToolsResult, McpError> {
        Ok(ListToolsResult::with_all_items(Self::tools()))
    }
    async fn call_tool(&self, request: CallToolRequestParams, context: RequestContext<RoleServer>) -> std::result::Result<CallToolResponse, McpError> {
        let args = request.arguments.map_or_else(|| Value::Object(Default::default()), Value::Object);
        match request.name.as_ref() {
            "todo_service-create_todo_v1" => {
                if let Ok(schema) = ElicitationSchema::from_json_schema(
                    serde_json::from_value(json!({
                        "type": "object",
                        "properties": {
                            "confirm": {"type": "string", "description": "Confirm creation.", "enum": ["yes", "no", ]},
                        },
                        "required": ["confirm", ]
                    })).unwrap()
                ) {
                    let params = ElicitRequestParams::FormElicitationParams {
                        meta: None,
                        message: "Please confirm the todo details before creating.".to_string(),
                        requested_schema: schema,
                    };
                    match context.peer.create_elicitation(params).await {
                        Ok(result) if result.action != ElicitationAction::Accept => {
                            return Ok(CallToolResult::success(vec![ContentBlock::text("Action cancelled by user.")]).into());
                        }
                        _ => {} // proceed (including errors — graceful degradation)
                    }
                }
                let result = self.inner.create_todo(args).await?;
                let text = serde_json::to_string(&result)
                    .map_err(|e| McpError::internal_error(format!("serialize response: {e}"), None))?;
                Ok(CallToolResult::success(vec![ContentBlock::text(text)]).into())
            }
            "todo_service-delete_todo_v1" => {
                if let Ok(schema) = ElicitationSchema::from_json_schema(
                    serde_json::from_value(json!({
                        "type": "object",
                        "properties": {
                            "confirm": {"type": "string", "description": "Confirm deletion.", "enum": ["yes", "no", ]},
                        },
                        "required": ["confirm", ]
                    })).unwrap()
                ) {
                    let params = ElicitRequestParams::FormElicitationParams {
                        meta: None,
                        message: "Are you sure you want to delete this todo? This action cannot be undone.".to_string(),
                        requested_schema: schema,
                    };
                    match context.peer.create_elicitation(params).await {
                        Ok(result) if result.action != ElicitationAction::Accept => {
                            return Ok(CallToolResult::success(vec![ContentBlock::text("Action cancelled by user.")]).into());
                        }
                        _ => {} // proceed (including errors — graceful degradation)
                    }
                }
                let result = self.inner.delete_todo(args).await?;
                let text = serde_json::to_string(&result)
                    .map_err(|e| McpError::internal_error(format!("serialize response: {e}"), None))?;
                Ok(CallToolResult::success(vec![ContentBlock::text(text)]).into())
            }
            "todo_service-get_todo_v1" => {
                let result = self.inner.get_todo(args).await?;
                let text = serde_json::to_string(&result)
                    .map_err(|e| McpError::internal_error(format!("serialize response: {e}"), None))?;
                Ok(CallToolResult::success(vec![ContentBlock::text(text)]).into())
            }
            "todo_service-list_todos_v1" => {
                let result = self.inner.list_todos(args).await?;
                let text = serde_json::to_string(&result)
                    .map_err(|e| McpError::internal_error(format!("serialize response: {e}"), None))?;
                Ok(CallToolResult::success(vec![ContentBlock::text(text)]).into())
            }
            "todo_service-update_todo_v1" => {
                if let Ok(schema) = ElicitationSchema::from_json_schema(
                    serde_json::from_value(json!({
                        "type": "object",
                        "properties": {
                            "confirm": {"type": "string", "description": "Confirm update.", "enum": ["yes", "no", ]},
                        },
                        "required": ["confirm", ]
                    })).unwrap()
                ) {
                    let params = ElicitRequestParams::FormElicitationParams {
                        meta: None,
                        message: "Please confirm the changes to this todo item.".to_string(),
                        requested_schema: schema,
                    };
                    match context.peer.create_elicitation(params).await {
                        Ok(result) if result.action != ElicitationAction::Accept => {
                            return Ok(CallToolResult::success(vec![ContentBlock::text("Action cancelled by user.")]).into());
                        }
                        _ => {} // proceed (including errors — graceful degradation)
                    }
                }
                let result = self.inner.update_todo(args).await?;
                let text = serde_json::to_string(&result)
                    .map_err(|e| McpError::internal_error(format!("serialize response: {e}"), None))?;
                Ok(CallToolResult::success(vec![ContentBlock::text(text)]).into())
            }
            _ => Err(McpError::internal_error(format!("unknown tool: {}", request.name), None)),
        }
    }

    async fn list_prompts(&self, _: Option<PaginatedRequestParams>, _: RequestContext<RoleServer>) -> std::result::Result<ListPromptsResult, McpError> {
        Ok(ListPromptsResult::with_all_items(Self::prompts()))
    }

    async fn get_prompt(&self, request: GetPromptRequestParams, _: RequestContext<RoleServer>) -> std::result::Result<GetPromptResponse, McpError> {
        for p in Self::prompts() {
            if p.name == request.name {
                let arg_str: String = request.arguments.as_ref()
                    .map(|a| a.iter().map(|(k, v)| format!("{k}={v}")).collect::<Vec<_>>().join(", "))
                    .unwrap_or_default();
                let desc = p.description.clone().unwrap_or_default();
                let messages = vec![PromptMessage::new_text(Role::User, format!("{desc} ({arg_str})"))];
                let mut result = GetPromptResult::new(messages);
                if let Some(ref d) = p.description {
                    result = result.with_description(d.clone());
                }
                return Ok(result.into());
            }
        }
        Err(McpError::invalid_params(format!("unknown prompt: {}", request.name), None))
    }

    async fn complete(&self, request: CompleteRequestParams, _: RequestContext<RoleServer>) -> std::result::Result<CompleteResult, McpError> {
        let key = match request.r#ref {
            Reference::Prompt(prompt_ref) => format!("{}:{}", prompt_ref.name, request.argument.name),
            _ => {
                let completion = CompletionInfo::with_all_values(Vec::new())
                    .map_err(|e| McpError::internal_error(format!("completion error: {e}"), None))?;
                return Ok(CompleteResult::new(completion));
            }
        };
        let values = Self::completion_map().get(&key).cloned().unwrap_or_default();
        let prefix = request.argument.value.to_lowercase();
        let filtered: Vec<String> = values
            .into_iter()
            .filter(|v| v.to_lowercase().starts_with(&prefix))
            .collect();
        let completion = CompletionInfo::with_all_values(filtered)
            .map_err(|e| McpError::internal_error(format!("completion error: {e}"), None))?;
        Ok(CompleteResult::new(completion))
    }

    async fn list_resources(&self, _: Option<PaginatedRequestParams>, _: RequestContext<RoleServer>) -> std::result::Result<ListResourcesResult, McpError> {
        Ok(ListResourcesResult::with_all_items(Self::resources()))
    }

    async fn list_resource_templates(&self, _: Option<PaginatedRequestParams>, _: RequestContext<RoleServer>) -> std::result::Result<ListResourceTemplatesResult, McpError> {
        Ok(ListResourceTemplatesResult::with_all_items(Self::resource_templates()))
    }

    async fn read_resource(&self, request: ReadResourceRequestParams, _: RequestContext<RoleServer>) -> std::result::Result<ReadResourceResponse, McpError> {
        let app_uri = app_resource_uri("TodoService");
        if request.uri == app_uri {
            let html = default_app_html("Todo App", "1.0.0", "A simple todo management application");
            return Ok(ReadResourceResult::new(vec![
                ResourceContents::text(html, request.uri).with_mime_type("text/html")
            ]).into());
        }
        Ok(ReadResourceResult::new(vec![ResourceContents::text("{}", request.uri)]).into())
    }
}

pub const TODO_SERVICE_MCP_DEFAULT_BASE_PATH: &str = "/todo/v1/todoservice/mcp";

pub struct TodoServiceMcpTransportConfig {
    pub transport: String,
    pub host: String,
    pub port: u16,
    pub base_path: String,
}

impl Default for TodoServiceMcpTransportConfig {
    fn default() -> Self {
        Self { transport: "streamable-http".into(), host: "0.0.0.0".into(), port: 8080,
               base_path: TODO_SERVICE_MCP_DEFAULT_BASE_PATH.into() }
    }
}

pub async fn serve_todo_service_mcp_stdio<T: TodoServiceMcpServer>(svc: T) -> std::result::Result<(), Box<dyn std::error::Error>> {
    let handler = TodoServiceMcpHandler::new(svc);
    handler.serve(rmcp::transport::stdio()).await?.waiting().await?;
    Ok(())
}

pub async fn serve_todo_service_mcp<T: TodoServiceMcpServer>(
    svc: T, config: TodoServiceMcpTransportConfig,
) -> std::result::Result<(), Box<dyn std::error::Error>> {
    let handler = TodoServiceMcpHandler::new(svc);
    let transports: Vec<&str> = config.transport.split(',').map(|s| s.trim()).filter(|s| !s.is_empty()).collect();
    let has_stdio = transports.iter().any(|t| *t == "stdio");
    let http_transport = transports.iter().find(|t| **t == "sse" || **t == "streamable-http").copied();

    if let Some(_ht) = http_transport {
        use rmcp::transport::streamable_http_server::{
            StreamableHttpServerConfig, StreamableHttpService, session::local::LocalSessionManager,
        };
        let h = handler.clone();
        let http_config = StreamableHttpServerConfig::default().with_sse_retry(None);
        let http_service = StreamableHttpService::new(
            move || Ok(h.clone()), Arc::new(LocalSessionManager::default()),
            http_config,
        );
        let app = axum::Router::new().nest_service(&config.base_path, http_service);
        let addr = format!("{}:{}", config.host, config.port);
        let listener = tokio::net::TcpListener::bind(&addr).await?;
        if has_stdio {
            tokio::spawn(async move { axum::serve(listener, app).await.expect("HTTP server failed"); });
        } else {
            axum::serve(listener, app).await?;
            return Ok(());
        }
    }

    if has_stdio {
        handler.serve(rmcp::transport::stdio()).await?.waiting().await?;
    } else if http_transport.is_none() {
        return Err(format!("unsupported transport(s): {}", config.transport).into());
    }
    Ok(())
}
